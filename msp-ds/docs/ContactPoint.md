---
category: Contact
---

ContactPoint — One row of a ContactPoints list — a cyan indicator square and mono copy, top-aligned so multi-line rows stay tidy.

## Usage

```tsx
<ContactPoint>We triage remotely first, dispatch onsite if needed</ContactPoint>
```

## Notes

Always compose inside a ContactPoints wrapper — that is what supplies the row spacing.
