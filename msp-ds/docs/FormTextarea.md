---
category: Forms
---

FormTextarea — Multi-line input, vertically resizable, 100px minimum — 120px inside a Form.

## Usage

```tsx
<FormTextarea id="detail" name="detail" rows={5} placeholder="Tell us what's broken and when it started." />
```
